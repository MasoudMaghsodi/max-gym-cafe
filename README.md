# 🍹 MAX GYM CAFE — منوی آنلاین

> منوی دیجیتال حرفه‌ای با قابلیت مدیریت آنلاین  
> تغییرات ادمین برای همه کاربران قابل مشاهده است

## ✨ ویژگی‌ها

- 🌐 **ذخیره‌سازی مشترک**: منو در GitHub ذخیره می‌شود و برای همه به‌روز است
- 🔐 **پنل ادمین امن**: فقط ادمین می‌تواند منو را ویرایش کند
- 📱 **Responsive**: کاملاً ریسپانسیو برای موبایل، تبلت و دسکتاپ
- 🎨 **طراحی مدرن**: انیمیشن‌های لطیف و UI زیبا
- 🎠 **Carousel خودکار**: نمایش پیشنهادات ویژه با قابلیت swipe
- 🔍 **جستجو و فیلتر**: جستجوی سریع و فیلتر بر اساس دسته‌بندی
- ⚡ **عملکرد سریع**: بهینه‌سازی شده با caching و lazy loading
- 🌍 **بدون نیاز به سرور**: کاملاً Static و قابل هاست روی GitHub Pages

---

## 🚀 نصب و راه‌اندازی

### 1. Clone کردن پروژه

```bash
git clone https://github.com/MasoudMaghsodi/max-gym-cafe.git
cd max-gym-cafe
```

### 2. اجرای محلی

```bash
python3 -m http.server 8000 --bind 127.0.0.1
```

سپس به آدرس `http://127.0.0.1:8000` بروید.

### 3. آپلود روی GitHub Pages

1. پروژه را در GitHub آپلود کنید
2. به Settings > Pages بروید
3. Source را روی `main` branch تنظیم کنید
4. منتظر بمانید تا deploy شود

---

## 🔑 تنظیم GitHub Token (برای ادمین)

برای اینکه تغییرات منو برای همه کاربران قابل مشاهده باشد، باید یک GitHub Personal Access Token تنظیم کنید:

### مراحل ساخت Token:

1. به [GitHub Settings > Developer settings > Personal access tokens > Tokens (classic)](https://github.com/settings/tokens) بروید
2. روی **Generate new token (classic)** کلیک کنید
3. یک نام برای token انتخاب کنید (مثلاً `max-cafe-admin`)
4. **Expiration** را روی `No expiration` یا مدت دلخواه تنظیم کنید
5. در قسمت **Scopes**، فقط `repo` را انتخاب کنید:
   - ✅ `repo` (Full control of private repositories)
6. روی **Generate token** کلیک کنید
7. Token را کپی کنید (فقط یک بار نمایش داده می‌شود!)

### استفاده از Token در پنل ادمین:

1. وارد پنل ادمین شوید
2. روی دکمه **⚙️ تنظیم GitHub Token** کلیک کنید
3. Token را paste کنید
4. حالا تغییرات شما در GitHub ذخیره می‌شود و برای همه قابل مشاهده است!

> [!IMPORTANT]
> **بدون Token**: تغییرات فقط در مرورگر شما ذخیره می‌شود (localStorage)  
> **با Token**: تغییرات در GitHub ذخیره شده و برای همه کاربران قابل مشاهده است

---

## 👨‍💼 ورود به پنل ادمین

1. روی دکمه **ادمین** در header کلیک کنید
2. اطلاعات ورود:
   - **نام کاربری**: `behzadcafeadmin`
   - **رمز عبور**: (رمز خود را وارد کنید)

> [!NOTE]
> برای تغییر رمز عبور، باید هش SHA256 رمز جدید را در فایل `modules/admin.js` قرار دهید.

---

## 📝 نحوه استفاده از پنل ادمین

### افزودن دسته‌بندی جدید
1. عنوان دسته را وارد کنید
2. نوع (گرم/سرد/اسموتی/غذا) را انتخاب کنید
3. روی **افزودن** کلیک کنید

### افزودن محصول جدید
1. دسته‌بندی را انتخاب کنید
2. نام، قیمت، مواد تشکیل‌دهنده و تگ‌ها را وارد کنید
3. تخفیف (اختیاری) را وارد کنید (0 تا 100)
4. لینک تصویر یا آپلود فایل تصویر
5. روی **ذخیره** کلیک کنید

### اعمال تخفیف گروهی
1. دسته‌بندی را انتخاب کنید
2. درصد تخفیف را وارد کنید
3. روی **اعمال** کلیک کنید

---

## 🛠️ تنظیمات پیشرفته

### تغییر اطلاعات GitHub Repository

فایل `modules/github-api.js` را باز کنید و اطلاعات زیر را تغییر دهید:

```javascript
const GITHUB_CONFIG = {
  owner: 'MasoudMaghsodi',  // نام کاربری GitHub خود
  repo: 'max-gym-cafe',      // نام repository
  branch: 'main',            // نام branch
  dataPath: 'data/menu.json' // مسیر فایل داده
};
```

### تغییر رمز عبور ادمین

1. رمز عبور دلخواه خود را انتخاب کنید
2. هش SHA256 آن را بگیرید (می‌توانید از [این سایت](https://emn178.github.io/online-tools/sha256.html) استفاده کنید)
3. در فایل `modules/admin.js` مقدار `ADMIN_PASS_SHA256` را تغییر دهید

---

## 📊 ساختار پروژه

```
max-gym-cafe/
├── index.html              # صفحه اصلی
├── assets/
│   ├── main.js            # فایل اصلی JavaScript
│   └── styles.css         # استایل‌های CSS
├── modules/
│   ├── admin.js           # پنل ادمین
│   ├── carousel.js        # کاروسل
│   ├── data.js            # مدیریت داده
│   ├── filters.js         # فیلتر و جستجو
│   ├── github-api.js      # اتصال به GitHub API
│   ├── render.js          # رندر کردن UI
│   └── utils.js           # توابع کمکی
├── data/
│   └── menu.json          # داده‌های منو (در GitHub)
└── README.md              # این فایل
```

---

## 🐛 رفع مشکلات رایج

### منو بارگذاری نمی‌شود
- اتصال اینترنت خود را بررسی کنید
- Console مرورگر را باز کنید (F12) و خطاها را بررسی کنید
- Cache مرورگر را پاک کنید و صفحه را رفرش کنید

### تغییرات ادمین ذخیره نمی‌شود
- مطمئن شوید که GitHub Token را تنظیم کرده‌اید
- بررسی کنید که Token معتبر و دارای دسترسی `repo` باشد
- اگر Token ندارید، تغییرات فقط در localStorage ذخیره می‌شود

### تصاویر نمایش داده نمی‌شوند
- لینک تصویر را بررسی کنید
- از لینک‌های HTTPS استفاده کنید
- از سرویس‌های معتبر مثل Unsplash استفاده کنید

---

## 🎯 ویژگی‌های تکنیکال

- ✅ **ES6 Modules**: کد modular و قابل نگهداری
- ✅ **GitHub API Integration**: ذخیره‌سازی مشترک بدون backend
- ✅ **Smart Caching**: کاهش API calls با caching 5 دقیقه‌ای
- ✅ **Error Handling**: مدیریت جامع خطاها
- ✅ **Input Validation**: اعتبارسنجی ورودی‌های کاربر
- ✅ **Loading States**: نمایش وضعیت بارگذاری
- ✅ **Debounced Search**: بهینه‌سازی جستجو
- ✅ **Accessibility**: پشتیبانی از keyboard navigation
- ✅ **RTL Support**: پشتیبانی کامل از راست به چپ

---

## 📞 تماس و پشتیبانی

- **مدیریت کافه**: بهزاد بهزادفر
- **تلفن**: ۰۹۱۴۴۸۱۶۴۲۷
- **آدرس**: میاندوآب، خیابان ۲۴ متری، روبه‌روی لوازم خانگی سرور
- **اینستاگرام**: [@max_gym_club](https://instagram.com/max_gym_club)
- **سازنده**: [MasoudMaghsodi](https://github.com/MasoudMaghsodi)

---

## 📄 لایسنس

این پروژه برای استفاده شخصی و تجاری کافه مکس ساخته شده است.

---

**ساخته شده با ❤️ و ☕ توسط [Masoud Maghsodi](https://github.com/MasoudMaghsodi)**
